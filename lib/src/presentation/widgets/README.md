# Widgets

This directory contains reusable UI components and custom widgets.

## Responsibilities
- Provide reusable UI components
- Implement custom widgets for common patterns
- Handle widget-level UI logic (not business logic)
- Maintain consistent design system components
- Reduce code duplication across pages

## Structure
Widgets should be self-contained and accept parameters for customization.
