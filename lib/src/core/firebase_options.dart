// Re-export the generated Firebase options so internal modules can import
// from `package:your_app/src/core/firebase_options.dart` without changing
// build outputs produced by the FlutterFire CLI.

export '../../firebase_options.dart';

// If you prefer to have the FlutterFire CLI output here directly, run:
//   flutterfire configure
// and copy the generated `DefaultFirebaseOptions` into this file.
